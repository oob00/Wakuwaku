spring:
  profiles:
    include: kakao
  datasource:
      driverClassName: org.h2.Driver
      url: jdbc:h2:mem:wakuwaku;MODE=MYSQL;
      username: sa
      password:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    open-in-view: false
    generate-ddl: false
  data:
    redis:
      host: localhost
      port: 6379

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.4

kafka:
  hosts: localhost:9092

jwt:
  secret: and0LWdlbmVyYXRlLXNlY3JldC1rZXktb29iLWt5dy1rbmV1ZHJmaA==